<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Email verify limit.
    |--------------------------------------------------------------------------
    |
    | Limitation time(minutes) for accept emails, when registration
    | a new account or add as the notification contacts.
    |
    */

    'verify_limit' => [
        'account' => 60,
        'contacts' => 360
    ],

    /*
    |--------------------------------------------------------------------------
    | iOS apps client id and client secret. >>> WIP <<<
    |--------------------------------------------------------------------------
    |
    */

    'ios_client_id' => env('IOS_CLIENT_ID', null),
    'ios_client_secret' => env('IOS_CLIENT_SECRET', null),

    /*
    |--------------------------------------------------------------------------
    | The time limit hours for the raise a alert.
    |--------------------------------------------------------------------------
    |
    */

    'time_limit_min' => 24,
    'time_limit_max' => 168,

    /*
    |--------------------------------------------------------------------------
    | Max count of make the device.
    |--------------------------------------------------------------------------
    |
    */

    'making_device_max' => [
        'basic' => 1,
        'business' => 30,
        'limited' => 0,
    ],

    /*
    |--------------------------------------------------------------------------
    | Max count of make the rule.
    |--------------------------------------------------------------------------
    |
    */

    'making_rule_max' => [
        'basic' => 5,
        'business' => 20,
        'limited' => 0,
    ],

    /*
    |--------------------------------------------------------------------------
    | Max count of make the contacts.
    |--------------------------------------------------------------------------
    |
    */

    'making_contacts_max' => [
        'basic' => 5,
        'business' => 30,
        'limited' => 0,
    ],

    /*
    |--------------------------------------------------------------------------
    | The max count of sending a alert mail.
    |--------------------------------------------------------------------------
    |
    */

    'send_notice_max' => [
        'basic' => 5,
        'business' => 10,
        'limited' => 10,
    ],

    /*
    |--------------------------------------------------------------------------
    | The max notify targets per device.
    |--------------------------------------------------------------------------
    |
    */

    'notify_targets_max' => [
        'basic' => 2,
        'business' => 5,
        'limited' => 0,
    ],

    /*
    |--------------------------------------------------------------------------
    | The interval minutes of accepting report from the device.
    |--------------------------------------------------------------------------
    |
    */

    'device_report_interval' => 60,

    /*
    |--------------------------------------------------------------------------
    | The interval minutes of send a verify url to contacts.
    |--------------------------------------------------------------------------
    |
    */

    'send_contacts_verify_interval' => 60,

    /*
    |--------------------------------------------------------------------------
    | The interval minutes of send alert notice mail.
    |--------------------------------------------------------------------------
    |
    */

    'send_alert_interval' => 60,
];
